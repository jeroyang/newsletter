============
Installation
============

At the command line::

    $ easy_install newsletter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv newsletter
    $ pip install newsletter
